
== Import ==

This assignment is submitted as a zipped Eclipse workspace project.
Unzip the zip file into its own directory.
This resulting file contains all of the files normally found in 
an Eclipse workspace except for the .metadata directory.

start Eclipse
File -> Import -> General -> "Existing Projects into Workspace" -> [Next]
"o - Select archive file" -> [Browse] -> {select the zip file} -> [Finish]

== Run ==

Use Eclipse to run the imported project.

== Export ==

The project originates in a git repository from which the
project is prepared for submission as follows:

Create a Zip archive that contains the contents of the latest commit on the current branch. 
The output format is inferred by the extension of the output file.

git archive -o /tmp/FredEiseleAssignment5.zip HEAD


